/**
 * Main class that runs the Perceptron based on user configurations loaded from a network configuration file. 
 * It stores and sets the configuration parameters, allocates memory for the Perceptron, trains/runs the 
 * Perceptron, and outputs relevant information from running the Perceptron.
 * 
 * @author Brenna Ren
 * @version September 9, 2025
 * Date of creation: September 5, 2025
 * 
 * Functions in this class:
 * 
 * 
 * 
 */
public class Main 
{

   /**
    * Main method that contains independent methods that run the Perceptron based on the user's configurations.
    * First, it sets the configuration parameters and echos them by printing them to the console. Then, it 
    * allocates memory for the Perceptron and trains/runs it (based on the user's configurations). Afterwards,
    * it will output the run results, which includes information such as training exit information, iterations
    * reached, and average error reached (user-selectable options).  
    * 
    * @param args 
    */
   public static void main(String[] args) 
   {
      Perceptron perceptron = new Perceptron();
   }
}